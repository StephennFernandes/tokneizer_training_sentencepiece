import os

# this would be the base dir
BASEPATH = "work"

DATA_DIR = os.path.join(BASEPATH, "data")
MODELS_DIR = os.path.join(BASEPATH, "trained_tokenizers")

HI_SENTENCES_DIR = os.path.join(DATA_DIR, "hi_sentences")
